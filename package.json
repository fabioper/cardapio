{
  "name": "cardapio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "turbo dev --parallel",
    "build": "turbo build --parallel",
    "test": "turbo test --parallel",
    "test:watch": "turbo test:watch --parallel",
    "test:e2e": "turbo test:e2e --parallel",
    "deploy:api": "turbo deploy --parallel"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.12.4"
  },
  "dependencies": {
    "@hookform/resolvers": "^3.3.4",
    "react-hook-form": "^7.51.2",
    "zod": "^3.22.4"
  }
}
